# IT2244-Day1-
The purpose of the "mkdr" function in a batch file is usually to create a directory  if it doesn't already exist. It's often a custom or shorthand function that wraps the mkdir command.

The purpose of a "Criteria" function in a batch file is not standard—it’s likely a function created by the script's author to check if certain conditions or criteria are met
